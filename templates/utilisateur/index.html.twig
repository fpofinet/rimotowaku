{% extends 'base.html.twig' %}

{% block title %}Hello UtilisateurController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
      
    <table class="table table-hover table-striped table-sm">
    <thead>
        <tr>
     
        <th scope="col">Nom</th>
        <th scope="col">Prenom</th>

        <th scope="col">Fonction</th>

        </tr>
    </thead>
    <tbody>
    {% for utilisateur in utilisateurs %}
        <tr class="">
        <td> {{utilisateur.nom}}</td>
        <td>{{utilisateur.prenom}}</td>

        <td>{{utilisateur.fonction}}</td>
        <td><a href={{ path("discussion",{'id':utilisateur.id})  }} class="btn"> Discussions </a></td>
        <td><a href={{ path("show_user", {'id':utilisateur.id})  }} class="btn btn-primary">...</a></td>
        </tr>
    {% endfor %}
    </tbody>
    </table>
{% endblock %}
